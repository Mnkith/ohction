<%= render 'form', user: @item %>

<%= button_to "Delete This Item", user_item_path(current_user, @item), method: :delete, controller: :items,
                                  data: { confirm: "You sure?" }, class:"btn btn-link d-inline-block" %>
